{
  "name": "di-loader",
  "version": "1.0.0",
  "description": "",
  "main": "lib/plugin.js",
  "scripts": {
    "start": "babel -s inline --presets es2015 --plugins add-module-exports -d lib -w src",
    "build": "babel -s inline --presets es2015 --plugins add-module-exports -d lib src",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jakwuh <jakwuh@gmail.ru>",
  "license": "ISC",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "dependencies": {
    "async": "^2.0.1",
    "di.js": "^1.9.1",
    "escodegen": "^1.8.1",
    "webpack": "^1.13.2"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-es2015": "^6.14.0"
  }
}
